kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: dev-app
  namespace: default
  labels:
    app.kubernetes.io/instance: dev-app
spec:
  to:
    kind: Service
    name: hello-openshift
  tls: {}
  port:
    targetPort: 8080
